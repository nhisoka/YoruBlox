#include <Windows.h>
#include <vector>

/*
* Here u paste ur init script as bytes and the size of the bytecode
* u can compile bytecode using Luau::compile
*/

std::vector<uint8_t> init_script_bytecode = {};

int init_script_size = 0x0;